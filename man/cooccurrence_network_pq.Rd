% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cooccurrence_network_pq.R
\name{cooccurrence_network_pq}
\alias{cooccurrence_network_pq}
\title{Probabilistic species co-occurrence from a phyloseq object}
\usage{
cooccurrence_network_pq(
  physeq,
  binary = TRUE,
  n_taxa = NULL,
  spp_names = TRUE,
  prob = "comb",
  thresh = TRUE,
  ...
)
}
\arguments{
\item{physeq}{(required) A \code{\link[phyloseq]{phyloseq-class}} object
obtained using the \code{phyloseq} package.}

\item{binary}{(logical, default TRUE) Convert the OTU table to
presence/absence before the analysis. Keep TRUE unless you know your data
suit the (rarely appropriate) abundance behaviour of \code{cooccur}.}

\item{n_taxa}{(int, default NULL) If set, keep only the \code{n_taxa} most
prevalent taxa before the analysis. \code{cooccur} scales quadratically with
the number of taxa, so capping is recommended on rich datasets.}

\item{spp_names}{(logical, default TRUE) Passed to \code{\link[cooccur:cooccur]{cooccur::cooccur()}}:
keep taxa names in the output.}

\item{prob}{(default "comb") The probability method passed to
\code{\link[cooccur:cooccur]{cooccur::cooccur()}}, either "comb" (combinatorics) or "hyper"
(hypergeometric).}

\item{thresh}{(logical, default TRUE) Passed to \code{\link[cooccur:cooccur]{cooccur::cooccur()}}:
remove taxa pairs expected to co-occur less than one time.}

\item{...}{Other parameters passed on to \code{\link[cooccur:cooccur]{cooccur::cooccur()}}.}
}
\value{
An object of class \code{cooccur} (see \code{\link[cooccur:cooccur]{cooccur::cooccur()}}). Use
\code{\link[cooccur:effect.sizes]{cooccur::effect.sizes()}}, \code{\link[cooccur:prob.table]{cooccur::prob.table()}} or \code{summary()} to
explore it, and \code{plot()} to visualise the association matrix.
}
\description{
\lifecycle{experimental}

Compute the probabilistic species co-occurrence model of Veech (2013) on
the taxa of a \code{phyloseq} object using \code{\link[cooccur:cooccur]{cooccur::cooccur()}}. Each pair of
taxa is classified as positively associated, negatively associated or
randomly associated given the observed co-occurrence across samples.

By default the OTU table is converted to presence/absence with
\code{\link[MiscMetabar:as_binary_otu_table]{MiscMetabar::as_binary_otu_table()}} before the analysis: \code{cooccur} is a
binary (incidence) model and gives spurious results on raw abundance data.
}
\examples{
if (requireNamespace("cooccur", quietly = TRUE)) {
  res <- cooccurrence_network_pq(data_fungi_mini, n_taxa = 30)
  summary(res)
  plot(res)
}
}
\references{
Veech, J. A. (2013) A probabilistic model for analysing species
co-occurrence. \doi{10.1111/j.1466-8238.2012.00789.x}.
}
\author{
Adrien Taudière
}
