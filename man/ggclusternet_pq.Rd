% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ggclusternet_pq.R
\name{ggclusternet_pq}
\alias{ggclusternet_pq}
\title{Microbial correlation network of a phyloseq object using ggClusterNet}
\usage{
ggclusternet_pq(
  physeq,
  group,
  n = 500,
  r_threshold = 0.6,
  p_threshold = 0.05,
  maxnode = 5,
  layout_net = "model_maptree2",
  big = TRUE,
  select_layout = TRUE,
  label = FALSE,
  zipi = FALSE,
  ...
)
}
\arguments{
\item{physeq}{(required) A \code{\link[phyloseq]{phyloseq-class}} object
obtained using the \code{phyloseq} package.}

\item{group}{(required) Name of the \code{sam_data} column defining the groups
for which a separate network is built.}

\item{n}{(int, default 500) Number of most-abundant taxa kept before the
analysis (the \code{N} argument of \code{ggClusterNet::network.2()}).}

\item{r_threshold}{(float, default 0.6) Minimum absolute correlation for an
edge to be kept.}

\item{p_threshold}{(float, default 0.05) Maximum p-value for an edge to be
kept.}

\item{maxnode}{(int, default 5) Maximum node size in the layout.}

\item{layout_net}{(default "model_maptree2") The \code{ggClusterNet} layout
algorithm.}

\item{big}{(logical, default TRUE) Use the large-network code path of
\code{ggClusterNet::network.2()}.}

\item{select_layout}{(logical, default TRUE) Let \code{ggClusterNet} adjust the
layout.}

\item{label}{(logical, default FALSE) Draw taxa labels on the network.}

\item{zipi}{(logical, default FALSE) Compute the within/among-module
connectivity (Zi-Pi) classification.}

\item{...}{Other parameters passed on to \code{ggClusterNet::network.2()}.}
}
\value{
The list returned by \code{ggClusterNet::network.2()}, whose first
element is a \code{ggplot2} network plot and whose remaining elements hold the
network and correlation data.
}
\description{
\lifecycle{experimental}

Build a per-group microbial correlation network from a \code{phyloseq} object
with \code{ggClusterNet::network.2()} (Wen et al. 2022). For each level of
\code{group}, taxa are correlated, thresholded on correlation strength and
significance, laid out with one of the \code{ggClusterNet} network layouts and
returned together with a \code{ggplot2} rendering.
}
\examples{
\dontrun{
# `ggClusterNet` is a GitHub package and needs WGCNA (Bioconductor):
# remotes::install_github("taowenmicro/ggClusterNet")
# pak::pkg_install("WGCNA")
data_fungi_mini_woNA4height <- subset_samples(
  data_fungi_mini,
  !is.na(data_fungi_mini@sam_data$Height)
)
res <- ggclusternet_pq(data_fungi_mini_woNA4height, group = "Height", n = 200)
res[[1]]
}
}
\references{
Wen, T. et al. (2022) ggClusterNet: an R package for microbiome network
analysis and modularity-based multiple network layouts.
\doi{10.1002/imt2.32}.
}
\author{
Adrien Taudière
}
