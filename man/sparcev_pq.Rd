% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sparcev_pq.R
\name{sparcev_pq}
\alias{sparcev_pq}
\title{Compositional taxa-environment correlation with SparCEV}
\usage{
sparcev_pq(physeq, variable, add_tax_table = TRUE, ...)
}
\arguments{
\item{physeq}{(required) A \code{\link[phyloseq]{phyloseq-class}} object
obtained using the \code{phyloseq} package.}

\item{variable}{(required) Name of the continuous variable in the
\code{sam_data} slot to correlate the taxa with.}

\item{add_tax_table}{(logical, default TRUE) Join the \code{tax_table} of
\code{physeq} to the result so each taxon carries its taxonomy.}

\item{...}{Other parameters passed on to \code{CompoCor::SparCEV()}.}
}
\value{
A data frame with one row per taxon: \code{taxa} (taxon name), \code{cor}
(the SparCEV correlation with \code{variable}) and \code{correlated} (logical, TRUE
when the correlation is deemed significant). When \code{add_tax_table = TRUE}
the columns of the \code{tax_table} are appended. The rendering of this result
(e.g. a labelled scatter plot) belongs in \code{ggplotpq}.
}
\description{
\lifecycle{experimental}

Compute the sparse correlation of each taxon with a continuous
environmental (sample) variable using \code{CompoCor::SparCEV()} (Sparse
Correlation with an Environmental Variable). SparCEV accounts for the
compositional nature of metabarcoding count data and flags the taxa whose
abundance is significantly correlated with the variable.
}
\examples{
\dontrun{
# `CompoCor` is a GitHub package:
# remotes::install_github("IbTJensen/CompoCor")
ps <- subset_samples(data_fungi_mini, !is.na(Time))
res <- sparcev_pq(ps, variable = "Time")
 res |>
   filter(correlated) |>
   ggplot(aes(x = cor, y = Family, shape = Guild, color = Order)) +
   geom_point() +
   geom_text(aes(label = taxa), nudge_y = 0.3, size = 2.5)
}
}
\seealso{
\code{\link[=cooccurrence_network_pq]{cooccurrence_network_pq()}}
}
\author{
Adrien Taudière
}
