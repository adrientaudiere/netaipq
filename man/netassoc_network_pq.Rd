% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/netassoc_network_pq.R
\name{netassoc_network_pq}
\alias{netassoc_network_pq}
\title{Species-association network of a phyloseq object using netassoc}
\usage{
netassoc_network_pq(
  physeq,
  numnulls = 100,
  method = "partial_correlation",
  plot = FALSE,
  verbose = FALSE,
  ...
)
}
\arguments{
\item{physeq}{(required) A \code{\link[phyloseq]{phyloseq-class}} object
obtained using the \code{phyloseq} package.}

\item{numnulls}{(int, default 100) Number of null-model replicates used to
build the expectation. Increase for real analyses; the default keeps the
examples and tests fast.}

\item{method}{(default "partial_correlation") Association measure passed to
\code{\link[netassoc:make_netassoc_network]{netassoc::make_netassoc_network()}}.}

\item{plot}{(logical, default FALSE) Draw the heatmaps produced by
\code{\link[netassoc:make_netassoc_network]{netassoc::make_netassoc_network()}}. FALSE by default to avoid a plotting
side effect; the returned object still contains the networks.}

\item{verbose}{(logical, default FALSE) Passed to
\code{\link[netassoc:make_netassoc_network]{netassoc::make_netassoc_network()}}.}

\item{...}{Other parameters passed on to
\code{\link[netassoc:make_netassoc_network]{netassoc::make_netassoc_network()}}.}
}
\value{
The list returned by \code{\link[netassoc:make_netassoc_network]{netassoc::make_netassoc_network()}}. Its
\code{network_all} element is an \code{igraph} graph of the significant
associations.
}
\description{
\lifecycle{experimental}

Infer a species-association network from a \code{phyloseq} object with
\code{\link[netassoc:make_netassoc_network]{netassoc::make_netassoc_network()}}. The method (Morueta-Holme et al. 2016)
compares the observed co-occurrence structure to a null model of random
assembly and keeps only the taxa pairs whose association departs
significantly from that expectation, returning a partial-correlation
network of the taxa.
}
\examples{
if (requireNamespace("netassoc", quietly = TRUE)) {
  ps <- prune_samples(sample_names(data_fungi_mini)[1:5], data_fungi_mini)
  ps <- clean_pq(ps, silent = TRUE)
  top_taxa <- names(sort(taxa_sums(ps), decreasing = TRUE))[1:20]
  ps <- prune_taxa(top_taxa, ps)
  res <- netassoc_network_pq(ps, numnulls = 20)

  netassoc::plot_netassoc_network(res$network_all)
}
}
\references{
Morueta-Holme, N. et al. (2016) A network approach for inferring species
associations from co-occurrence data. \doi{10.1111/ecog.01892}.
}
\seealso{
\code{\link[=cooccurrence_network_pq]{cooccurrence_network_pq()}}, \code{\link[=bipartite_network_pq]{bipartite_network_pq()}}
}
\author{
Adrien Taudière
}
